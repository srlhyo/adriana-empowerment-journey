{
  "files": [
    "next-env.d.ts"
  ],
  "references": [
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.node.json"
    }
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    },
    // noImplicitAny, noUnusedParameters, noUnusedLocals, strictNullChecks are effectively
    // controlled by tsconfig.app.json for the app code, or default to stricter values
    // when not specified here for other contexts (like tsconfig.node.json).
    // It's cleaner to remove them from this root tsconfig if they were set to non-strict values,
    // as the referenced configs (app, node) will define their own strictness.
    // skipLibCheck and allowJs are not directly part of "strict" mode and can be kept.
    "skipLibCheck": true,
    "allowJs": true,
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "strict": false,
    "noEmit": true,
    "incremental": true,
    "module": "esnext",
    "esModuleInterop": true,
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "plugins": [
      {
        "name": "next"
      }
    ],
    "strictNullChecks": true
  },
  "include": [
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": [
    "node_modules"
  ]
}
